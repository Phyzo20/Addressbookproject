pipeline {
    agent any
    tools {
    maven 'mymaven'
    }
    stages {
        stage('build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('deploy') {
            steps {
                sshagent(['techportsjen']) {
    //scp <src_dir> user@tomcatIP:/tomcat/webapps
    sh "scp -o StrictHostKeyChecking=no target/*.war techports@34.201.7.185:/home/techports/tomcat/webapps"
}
            }
        }
        
    }
}
